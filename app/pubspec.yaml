name: attendance_gateway
description: Zero-Trust Student Attendance Gateway
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.4.3
  flutter_secure_storage: ^9.2.2
  mobile_scanner: ^7.0.0
  device_info_plus: ^10.1.0
  permission_handler: ^11.3.1
  crypto: ^3.0.3
  convert: ^3.1.1
  uuid: ^4.4.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true