# secrets
.env
*.env
!*.env.example

# node
node_modules/
dist/
build/

# flutter/dart
.dart_tool/
.packages
pubspec.lock
app/build/

# backups (never commit)
deploy/supabase/volumes/
*.dump

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# shared client API key (never commit)
.api_key

# Release APKs (too large for git - distribute separately)
releases/*.apk
